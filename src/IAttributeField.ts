export interface IAttributeField {
    controlType: number
    defaultValue: string
    predicates: any
    placeHolder: string
    isEnabled: boolean
    claimId: string
    template: string
    displayName: string
}